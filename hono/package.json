{
  "name": "hono",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot main.ts",
    "start": "prisma generate --no-engine && bun --bun run  main.ts",
    "db:gen": "prisma generate --no-engine",
    "db:push": "prisma db push",
    "db:up": "prisma generate && prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "better-auth": "^1.3.34",
    "dotenv": "^17.2.3",
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "prisma": "^6.19.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
