{
  "name": "hono",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot main.ts",
    "start": "bun db:up && bun --bun run  main.ts",
    "db:gen": "prisma generate --no-engine",
    "db:push": "prisma db push",
    "db:up": "prisma generate --no-engine && prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "better-auth": "^1.3.34",
    "dotenv": "^17.2.3",
    "ioredis": "^5.8.2",
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "prisma": "^6.19.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
