[phases.setup]
nixPkgs = ["nodejs_24", "curl", "bun] 
nixLibs = [] 
aptPkgs = []   

[phases.install]
cmds = ["bun install"]
dependsOn = ["setup"]

[phases.build]
cmds = ["bun run build"]
dependsOn = ["install"]

[start]
cmd = "bun start"

# [nix]
# pkgs = ["nodejs_24", "curl", "bun"]

[variables]
NODE_ENV = "production"
